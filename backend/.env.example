# PostgreSQL - en local
DB_HOST=localhost
DB_PORT=5432
DB_NAME=texte_new
DB_USER=postgres
DB_PASSWORD=ton_mot_de_passe

# PostgreSQL - en production (Render le fournit automatiquement)
# DATABASE_URL=postgres://user:password@host/db

# Serveur
PORT=3000
API_KEY_IOT=geoalerte_iot_key_2024

# JWT (change this in production!)
JWT_SECRET=change_this_secret_in_production

# Origines autorisées (CORS) - mets l'URL de ton site Netlify
ALLOWED_ORIGINS=http://localhost:5500,https://ton-site.netlify.app

# Twilio SMS (optionnel)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

NODE_ENV=development
